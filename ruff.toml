# Repo-wide ruff baseline (lint + format). Package pyproject.toml files may
# tighten further. Default lint select (E4/E7/E9 + F) — start permissive,
# ratchet tighter as the codebase allows.
target-version = "py311"
