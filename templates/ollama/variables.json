{
  "OLLAMA_PORT": {
    "type": "text",
    "description": "Host port Ollama binds on. Default-bound to 127.0.0.1 (see template.yml — OLLAMA_HOST). For LAN/public access put NPM + Authelia in front, do not change this to 0.0.0.0.",
    "default": "11434"
  },
  "OLLAMA_DEFAULT_MODEL": {
    "type": "text",
    "description": "Primary model — the tag the Solaris Engine's `model.model` points at after install. Use any Ollama library tag (https://ollama.com/library) or a user-namespaced tag like `gemma4:12b`, `gemma4:e2b`, or `VladimirGav/gemma4-26b-16GB-VRAM:latest`. Default `gemma4:12b` is the household-stack sweet spot: 12B params, fits on a 16 GB GPU, AND ships native multimodal capabilities (`ollama show gemma4:12b` reports `completion, vision, audio, tools, thinking`). That single tag covers what we used to need a separate vision pull for (#923 / #938).",
    "default": "gemma4:12b"
  },
  "OLLAMA_EXTRA_MODELS": {
    "type": "text",
    "description": "Additional models to pre-pull on first install (comma-separated tags), on top of OLLAMA_DEFAULT_MODEL. The point is to give the operator one-click switchable alternatives in the Solaris Engine's model settings without a fresh download. Default `gemma4:e4b`: the Solaris household FAST_MODEL (the voice/chat hot path) — pulling it here makes the bundled stack work out of the box, since OLLAMA_DEFAULT_MODEL only ships the thorough gemma4:12b. Add `VladimirGav/gemma4-26b-16GB-VRAM:latest` or any other tag to pre-pull more. CAVEAT: the aggressive quantization of the 26B-VRAM variant strips vision and audio from its `Capabilities` block. Switching the active model to that tag gives up multimodal — keep `gemma4:12b` or `gemma4:e4b` for OCR / voice-note flows. Set to empty string to skip extras.",
    "default": "gemma4:e4b"
  },
  "OLLAMA_VISION_MODEL": {
    "type": "text",
    "description": "Optional second model pulled on first start, mainly historical now that the default OLLAMA_DEFAULT_MODEL (`gemma4:12b`) ships vision + audio natively. Leave blank in normal household installs — only set this if you've changed OLLAMA_DEFAULT_MODEL to a text-only tag and still want Solaris's `media-ingestion-multimodal` skill to have a vision backend. Suggested non-default tags: `qwen2.5vl:7b` (Apache-2, ~6 GB), `llava:13b` (~8 GB), `bakllava:7b` (~5 GB).",
    "default": ""
  },
  "OLLAMA_GPU_PASSTHROUGH": {
    "type": "text",
    "description": "Blank = auto-detect: post-deploy engages NVIDIA GPU passthrough when the host has a CDI-registered GPU (`/etc/cdi/nvidia.yaml`, from `nvidia-ctk cdi generate`), otherwise CPU-only. Set `yes` to force GPU or `no` to force CPU. Actual passthrough is delivered by post-deploy's #1026 fixup, which swaps the .kube unit for a `.container` Quadlet with `AddDevice=nvidia.com/gpu=all` (podman silently drops the manifest's `resources.limits.nvidia.com/gpu`, so the fixup — not the rendered block — is what gets the device).",
    "default": ""
  },
  "OLLAMA_CONTEXT_LENGTH": {
    "type": "text",
    "description": "Ollama's DEFAULT load context window (tokens) — forces the size a model is loaded at, regardless of per-request hints (`/v1/chat/completions` silently ignores per-request num_ctx — only this env-set DEFAULT lands, #146; oversized `/api/chat` requests are clamped to it, box-observed: a num_ctx=262144 request loaded at the env value). Default `32768`: a lean base prompt (≤3k tokens system+tools) leaves ~29k conversation room and the chat client auto-compacts at 90% — the old `131072` default existed only because the Hermes-era base prompt had grown to ~25k tokens, which crowded a 32k window. Box-measured (#214): gemma4:12b @32768 ≈8.95 GB vs ≈10.3 GB @131072 — the saved ~1.4 GB of KV budget is what lets gemma4:12b + gemma4:e2b + nomic-embed-text stay co-resident on the 16 GB GPU (see OLLAMA_MAX_LOADED_MODELS=3). Raise only if a workload genuinely needs longer context AND you reduce co-residency to match.",
    "default": "32768"
  },
  "OLLAMA_KEEP_ALIVE": {
    "type": "text",
    "description": "How long Ollama keeps a model loaded in VRAM after its last request (Go duration, e.g. `24h`, `60m`; `-1` = forever, `0` = unload immediately). Stock default is `5m`, which evicts the chat model after a short pause → the next turn pays a cold model RELOAD (several seconds) on top of prefill. Default `24h`: a normal conversational pause — and an overnight gap — never triggers a cold reload, so the cached prefix and the loaded weights survive (#268 anti-eviction). At OLLAMA_CONTEXT_LENGTH=32768 all three resident models (see OLLAMA_MAX_LOADED_MODELS=3) fit with headroom, so long pinning carries no OOM risk.",
    "default": "24h"
  },
  "OLLAMA_MAX_LOADED_MODELS": {
    "type": "text",
    "description": "Maximum number of models Ollama keeps loaded at once (per Ollama's `OLLAMA_MAX_LOADED_MODELS`; `0` = unlimited; GLOBAL cap counting EVERY model — the embed model is NOT exempt, box-measured 2026-06-10). Default `2`: the household fast chat is now gemma4:e4b (box-measured 3.3 GB @ ctx 32768, vs gemma4:e2b's 2.0 GB) — strong enough to drive the multi-step voice flows e2b fabricated (#396). With e4b, the would-be trio e4b + gemma4:12b (8.4 GB) + nomic-embed-text (0.3 GB) ≈ 12 GB of weights climbs to ≈16.2 GB resident with KV/runner overhead at ctx 32768 — past the 16 GB GPU's safe edge (risks an OOM drop to CPU). So cap at `2`: e4b + embed stay hot on the voice path; 12b loads on demand for the Gründlich mode and the night crons, evicting e4b — the first fast turn after a cron night then pays a ~6.75s reload (the accepted trade for staying within VRAM). Raise to `3` only if you also trim OLLAMA_CONTEXT_LENGTH so the trio fits.",
    "default": "2"
  },
  "OLLAMA_FLASH_ATTENTION": {
    "type": "text",
    "description": "Enables Ollama's flash-attention kernel (`1` on, `0` off). Speed change is negligible on this GPU class (RTX 2000 Ada) but it's harmless and is the prerequisite for optional KV-cache quantization, so it's on by default.",
    "default": "1"
  },
  "OLLAMA_EMBED_MODEL": {
    "type": "text",
    "description": "Dedicated embedding model, pre-pulled at install so it's resident alongside the chat model. Embeddings/RAG must target THIS tag, never OLLAMA_DEFAULT_MODEL: Ollama runs a separate llama-server runner per model, so an embed request to a different model runs in PARALLEL with a chat generation (measured: an embed during a 40s 12b generation returned in 1.79s with no stall), whereas a second request to the chat model SERIALIZES behind the first. Do NOT raise OLLAMA_NUM_PARALLEL (it multiplies KV-cache VRAM and slows concurrent gens) and do NOT run a second Ollama — a distinct embed tag is the whole fix (#214). Default `nomic-embed-text` (~274 MB).",
    "default": "nomic-embed-text"
  },
  "OLLAMA_READINESS_TIMEOUT_SECONDS": {
    "type": "text",
    "description": "Stall window for model pulls, in seconds: a pull is abandoned only after this long with NO download progress — not on total elapsed time, so a slow link pulling a multi-GB model for an hour is fine as long as bytes keep flowing. Also caps the initial wait for the Ollama API to answer (at most 120s). Raise it if your connection has long stalls; lower it to fail faster on a dead mirror.",
    "default": "600"
  },
  "OLLAMA_SUBDOMAIN": {
    "type": "subdomain",
    "description": "Subdomain for the Ollama API. Internal exposure — gets a cert and a LAN-only access list, plus Authelia forward-auth on top because Ollama itself ships no auth. Use https://<OLLAMA_SUBDOMAIN>.<PUBLIC_DOMAIN>/api/generate from any LAN client; mobile/external clients get a 403 from NPM and a 302-to-Authelia from any in-LAN unauthenticated request.",
    "default": "ollama",
    "exposure": "internal",
    "proxyPort": "OLLAMA_PORT",
    "loopbackOnly": true,
    "proxyConfig": {
      "allow_websocket_upgrade": true,
      "block_exploits": true,
      "ssl_forced": true,
      "advanced_config": "__authelia_forward_auth__"
    }
  }
}
